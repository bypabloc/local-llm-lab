# Read the documentation before extending the defaults:
# https://docs.searxng.org/admin/settings/

use_default_settings: true

server:
  secret_key: "local-llm-lab-dev-only-not-a-real-secret"
  image_proxy: true

search:
  formats:
    - html
    - json
